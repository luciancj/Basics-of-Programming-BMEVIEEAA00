/*
 * Problem 3.18
 * Write a C program that reads an unknown number of disk-usage percentages (integers, 0–100) until the user enters -1 (sentinel), counts the categories (safe <50%, caution 50–80%, critical >80%), and...
 */

#include <stdio.h>

int main() {
    // TODO: Implement solution
    
    return 0;
}
